var React = require('react')

var Countdown = React.createClass({
  render: () => {
      return (
        <div>
            Countdown component
        </div>
      )
  }
})

module.exports = Countdown;
